# Clé secrète de Django (ne pas utiliser cette valeur en production)
SECRET_KEY='votre-cle-secrete-django-ici'

# Mode débogage (True en développement, False en production)
DEBUG=True

# Configuration de la base de données MySQL
DB_NAME=portfolio_db
DB_USER=root
DB_PASSWORD=votre_mot_de_passe
DB_HOST=127.0.0.1
DB_PORT=3306

# Domaines autorisés pour CORS (séparés par une virgule)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
